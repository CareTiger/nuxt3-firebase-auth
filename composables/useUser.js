export const useGetUser = (user) => {
    // get/cretae user profile from API connected to database
    console.log(
        'get/cretae latest user profile and other data like notifications from API connected to database'
    );
    // console.log('useGetUser', user);
};
